<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ChatApplication" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="materialThemeOverlay">@style/ThemeOverlay.Material3</item>
    </style>

    <style name="ThemeOverlay.Material3" parent="Theme.Material3.DayNight.NoActionBar"/>

</resources>